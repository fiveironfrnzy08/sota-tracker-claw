fastmcp>=2.0,<3.0
aiohttp>=3.9
huggingface_hub>=0.20
python-dotenv>=1.0
# Pinned: the browser binary is a separate download (`playwright install`) tied to
# the package's expected build. A floating range let the package jump ahead of the
# installed browser, silently crashing the AA/LMArena scrapers for ~a month. Bump
# this and the pinned build together, and start.sh self-heals the browser on boot.
playwright==1.58.0
fastapi>=0.100.0
uvicorn>=0.23.0
slowapi>=0.1.9
